# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
pydantic>=2.7
python-multipart>=0.0.9

# LangChain / LangGraph
langchain>=0.3
langchain-classic>=0.1
langchain-openai>=0.2
langchain-community>=0.3
langchain-qdrant>=0.2
langchain-text-splitters>=0.3
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
aiosqlite>=0.20

# Vector DB
qdrant-client>=1.11

# Document Processing
pymupdf>=1.28
pymupdf4llm>=1.28        # layout-aware markdown extraction (multi-column, headings)
Pillow>=10.4
pandas>=2.0              # table.to_pandas() in document_pipeline.py
tabulate>=0.9            # pandas .to_markdown() needs this

# Embeddings (local, free — used in all pipeline modes)
FlagEmbedding>=1.2       # BAAI/bge-m3 dense embedding (1024-dim, 8192 context, multilingual)
transformers>=4.40,<5.0  # FlagEmbedding 1.x requires transformers 4.x (5.x dropped prepare_for_model)
fastembed>=0.3           # Qdrant/bm25 sparse vectors (BM25 hybrid search)
# Note: Cohere reranker uses httpx (no separate cohere package needed)

# Evaluation
deepeval>=1.4

# Dev / utilities
httpx>=0.27
